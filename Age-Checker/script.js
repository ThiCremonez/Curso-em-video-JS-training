function ver() {
    var date = new Date()
    var year = data.getFullYear()
    var Fyear = document.getElementById('txtyear')
    var res = document.querySelector('div#res')
    if (fyear.value.length == 0 || fyear.value > ano){
        window.alert(verificar e tentar novamente)
    }

}
